package org.learn.interfaz.ejemplo.granja;
// interface
interface Animal {
  public void animalSound(); // interface method (does not have a body)
  public int move(); // interface method (does not have a body)
}